services:
  - type: web
    name: sui-wallet-monitor
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: WALLET_ADDRESS
        value: '@pawtato-land'
